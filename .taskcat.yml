project:
        #auth:
        #us-east-2: test
  name: taskcat-example-vpc
  regions:
    - us-east-2
tests:
  sqs-test:
    auth:
      us-east-2: test
    template: ./vpc.yml
    # s3_bucket: taskcattestarjun
